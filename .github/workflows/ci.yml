name: CI

on:
  push:
    branches:
    - main

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install dependencies
      run: |
        pip install flit
        flit install
    - name: run tests
      run: pytest -vv
